services:
  app:
    container_name: onespace-scheduling-agent
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3011}:${PORT:-3011}"
    env_file:
      - .env
    restart: unless-stopped
